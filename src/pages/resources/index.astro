---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Free Vietnamese Learning Tools & Practice Resources | Havie" 
  description="Master Vietnamese online with free interactive tools: Vocabulary Lab, 6 Tones Audio Studio, Pronoun Calculator, and Conversation Practice."
  canonical="https://havielearn.com/resources"
>
  <Fragment slot="head">
    <style>
      .curved-underline {
        position: relative;
        display: inline-block;
      }
      .curved-underline::after {
        content: "";
        position: absolute;
        left: 0;
        bottom: -4px;
        width: 100%;
        height: 6px;
        border-bottom: 3.5px solid #ef7218;
        border-radius: 50%;
        transform: rotate(-1.5deg);
      }
    </style>
  </Fragment>

  <!-- BACKGROUND GLOW -->
  <div class="absolute inset-0 pointer-events-none -z-10 overflow-hidden">
    <div class="absolute top-10 left-1/2 -translate-x-1/2 w-[550px] h-[550px] hero-glow rounded-full blur-3xl"></div>
  </div>

  <!-- NAVBAR -->
  <header class="sticky top-0 z-40 bg-[#fffaf5]/90 backdrop-blur-md border-b border-stone-200/80 h-20">
    <div class="max-w-7xl mx-auto px-5 sm:px-8 lg:px-12 h-full flex items-center justify-between">
      
      <!-- LOGO BÊN TRÁI -->
      <a href="/" class="group flex flex-col select-none text-left shrink-0">
        <div class="h-7 sm:h-8 flex items-center">
          <svg class="h-7 sm:h-8 w-auto overflow-visible" viewBox="0 0 175 30" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M0 2H6V12H18V2H24V28H18V17H6V28H0V2Z" fill="#dc2626"/>
            <path d="M41 2L52 28H45.5L43.2 22H33.8L31.5 28H25L36 2H41ZM38.5 8.5L35.2 17.5H41.8L38.5 8.5Z" fill="#dc2626"/>
            <path d="M53 2H59.5L67 21L74.5 2H81L70.5 28H63.5L53 2Z" fill="#dc2626"/>
            <path d="M86 2H92V28H86V2Z" fill="#dc2626"/>
            <path d="M98 2H117V7.5H104V12H115V17H104V22.5H117V28H98V2Z" fill="#dc2626"/>
          </svg>
        </div>
        <span class="text-[9.5px] sm:text-[10px] font-bold tracking-[0.22em] sm:tracking-[0.26em] text-stone-400 uppercase mt-1 pl-0.5 leading-none">
          Learn Vietnamese Online
        </span>
      </a>

      <!-- CỤM MENU CHỮ Ở GIỮA -->
      <nav class="hidden md:flex items-center space-x-7 lg:space-x-8 text-base font-bold text-slate-800">
        <a href="/#about" class="hover:text-red-600 transition">About Me</a>
        <a href="/#curriculum" class="hover:text-red-600 transition">Learning Tracks</a>
        <a href="/resources" class="text-red-600 transition font-bold">Resources</a>
        <a href="/student" class="hover:text-red-600 transition">Student Portal</a>
        <a href="/test" class="hover:text-red-600 transition">Level Test</a>
      </nav>

      <!-- NÚT BOOK -->
      <div class="hidden md:flex items-center pl-6 shrink-0">
        <button
          type="button"
          class="open-modal-trigger inline-flex items-center justify-center px-5 py-3 text-sm font-extrabold text-white bg-red-600 hover:bg-red-700 rounded-xl shadow-lg shadow-red-600/25 transition hover:-translate-y-0.5 cursor-pointer"
        >
          Book 25-Min Trial Lesson →
        </button>
      </div>

      <!-- MOBILE TOGGLE BUTTON -->
      <div class="flex items-center md:hidden">
        <button 
          id="mobileMenuBtn" 
          type="button" 
          aria-label="Toggle Menu"
          class="w-10 h-10 rounded-xl bg-stone-100 hover:bg-stone-200 text-slate-800 flex items-center justify-center font-bold transition cursor-pointer"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>

    </div>

    <!-- Mobile Dropdown Menu -->
    <div id="mobileMenu" class="hidden md:hidden absolute top-20 left-0 w-full bg-[#fffaf5] border-b border-stone-200 shadow-xl px-6 py-5 space-y-4 font-semibold text-slate-800 text-base">
      <a href="/#about" class="mobile-link block hover:text-red-600 transition">About Me</a>
      <a href="/#curriculum" class="mobile-link block hover:text-red-600 transition">Learning Tracks</a>
      <a href="/resources" class="mobile-link block text-red-600 font-bold transition">Resources</a>
      <a href="/student" class="mobile-link block hover:text-red-600 transition">Student Portal</a>
      <a href="/test" class="mobile-link block hover:text-red-600 transition">Level Test</a>
      <div class="pt-3 border-t border-stone-200">
        <button
          type="button"
          class="open-modal-trigger w-full py-3.5 text-sm font-bold text-white bg-red-600 rounded-xl text-center shadow-md cursor-pointer"
        >
          Book 25-Min Trial Lesson →
        </button>
      </div>
    </div>
  </header>

  <main class="flex-grow pt-10 sm:pt-14 pb-20 sm:pb-24">
    
    <!-- HEADER INTRO -->
    <section class="max-w-4xl mx-auto px-5 sm:px-8 text-center mb-12 sm:mb-16 space-y-4">
      <div class="inline-flex items-center gap-2.5 px-4 py-2 rounded-full bg-white border border-stone-200/95 text-xs sm:text-sm font-extrabold text-slate-800 shadow-2xs">
        <span class="w-2.5 h-2.5 rounded-full bg-red-600 animate-pulse"></span>
        <span>Free Vietnamese Learning Resources & Tools</span>
      </div>

      <h1 class="text-3xl sm:text-5xl lg:text-5xl font-extrabold text-slate-900 tracking-tight font-brand leading-[1.18]">
        Master Spoken Vietnamese with <span class="curved-underline text-red-600">Interactive Tools.</span>
      </h1>
      
      <p class="text-stone-600 text-base sm:text-lg max-w-xl mx-auto leading-relaxed font-medium">
        Skip the boring books. Use these free online tools to practice vocabulary games, tones, pronouns, and speak naturally like a local.
      </p>
    </section>

    <!-- RESOURCES CARDS (ĐÃ TĂNG CỠ CHỮ VỪA PHẢI) -->
    <section class="max-w-6xl mx-auto px-5 sm:px-8 lg:px-12 pb-10">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 sm:gap-8">
        
        <!-- 1. Vocabulary Lab Card -->
        <a href="/resources/vocabulary?topic=numbers_expert" class="group bg-[#fffaf5] rounded-[32px] p-7 sm:p-9 border border-stone-200/90 shadow-lg shadow-stone-200/40 hover:shadow-2xl hover:border-red-300 hover:-translate-y-1.5 transition-all duration-300 flex flex-col justify-between relative overflow-hidden">
          <div class="absolute top-0 right-0 w-32 h-32 bg-red-100/50 rounded-bl-full pointer-events-none group-hover:scale-110 transition-transform"></div>

          <div class="relative z-10 space-y-4">
            <div class="w-14 h-14 rounded-2xl bg-red-100 text-red-600 flex items-center justify-center text-2xl shadow-xs group-hover:rotate-6 transition-transform">
              🧩
            </div>
            <div>
              <span class="text-[11px] font-extrabold text-red-600 uppercase tracking-wider bg-red-50 px-2.5 py-1 rounded-md border border-red-200/60 inline-block mb-2">Interactive Games</span>
              <h2 class="text-xl sm:text-2xl font-extrabold text-slate-900 group-hover:text-red-600 transition-colors font-brand">Vocabulary Lab</h2>
              <p class="text-sm sm:text-base text-stone-600 leading-relaxed mt-2 font-normal">
                Practice everyday topics with flashcards, matching games, multiple-choice quizzes, word unscramble, and true/false questions.
              </p>
            </div>
          </div>

          <div class="mt-8 pt-4 border-t border-stone-200/70 flex items-center justify-between text-xs sm:text-sm font-bold text-slate-800 group-hover:text-red-600 transition-colors relative z-10">
            <span>Launch Lab Games</span>
            <span class="group-hover:translate-x-1.5 transition-transform text-base">→</span>
          </div>
        </a>

        <!-- 2. Tones Card -->
        <a href="/resources/tones" class="group bg-[#fffaf5] rounded-[32px] p-7 sm:p-9 border border-stone-200/90 shadow-lg shadow-stone-200/40 hover:shadow-2xl hover:border-emerald-300 hover:-translate-y-1.5 transition-all duration-300 flex flex-col justify-between relative overflow-hidden">
          <div class="absolute top-0 right-0 w-32 h-32 bg-emerald-100/50 rounded-bl-full pointer-events-none group-hover:scale-110 transition-transform"></div>
          
          <div class="relative z-10 space-y-4">
            <div class="w-14 h-14 rounded-2xl bg-emerald-100 text-emerald-700 flex items-center justify-center text-2xl shadow-xs group-hover:rotate-6 transition-transform">
              🎵
            </div>
            <div>
              <span class="text-[11px] font-extrabold text-emerald-700 uppercase tracking-wider bg-emerald-50 px-2.5 py-1 rounded-md border border-emerald-200/60 inline-block mb-2">Pronunciation</span>
              <h2 class="text-xl sm:text-2xl font-extrabold text-slate-900 group-hover:text-emerald-700 transition-colors font-brand">Tones Studio</h2>
              <p class="text-sm sm:text-base text-stone-600 leading-relaxed mt-2 font-normal">
                Master the 6 Vietnamese tones with clear audio, visual pitch guides, and easy practice exercises.
              </p>
            </div>
          </div>

          <div class="mt-8 pt-4 border-t border-stone-200/70 flex items-center justify-between text-xs sm:text-sm font-bold text-slate-800 group-hover:text-emerald-700 transition-colors relative z-10">
            <span>Explore Tones</span>
            <span class="group-hover:translate-x-1.5 transition-transform text-base">→</span>
          </div>
        </a>

        <!-- 3. Pronouns Card -->
        <a href="/resources/pronouns" class="group bg-[#fffaf5] rounded-[32px] p-7 sm:p-9 border border-stone-200/90 shadow-lg shadow-stone-200/40 hover:shadow-2xl hover:border-blue-300 hover:-translate-y-1.5 transition-all duration-300 flex flex-col justify-between relative overflow-hidden">
          <div class="absolute top-0 right-0 w-32 h-32 bg-blue-100/50 rounded-bl-full pointer-events-none group-hover:scale-110 transition-transform"></div>

          <div class="relative z-10 space-y-4">
            <div class="w-14 h-14 rounded-2xl bg-blue-100 text-blue-700 flex items-center justify-center text-2xl shadow-xs group-hover:rotate-6 transition-transform">
              👥
            </div>
            <div>
              <span class="text-[11px] font-extrabold text-blue-700 uppercase tracking-wider bg-blue-50 px-2.5 py-1 rounded-md border border-blue-200/60 inline-block mb-2">Social Etiquette</span>
              <h2 class="text-xl sm:text-2xl font-extrabold text-slate-900 group-hover:text-blue-700 transition-colors font-brand">Pronoun Matrix</h2>
              <p class="text-sm sm:text-base text-stone-600 leading-relaxed mt-2 font-normal">
                Never get confused by "I" and "You". Instantly find out whether to use Anh, Chị, or Em.
              </p>
            </div>
          </div>

          <div class="mt-8 pt-4 border-t border-stone-200/70 flex items-center justify-between text-xs sm:text-sm font-bold text-slate-800 group-hover:text-blue-700 transition-colors relative z-10">
            <span>Open Calculator</span>
            <span class="group-hover:translate-x-1.5 transition-transform text-base">→</span>
          </div>
        </a>

        <!-- 4. Daily Conversations Card -->
        <a href="/resources/daily-conversation" class="group bg-[#fffaf5] rounded-[32px] p-7 sm:p-9 border border-stone-200/90 shadow-lg shadow-stone-200/40 hover:shadow-2xl hover:border-amber-300 hover:-translate-y-1.5 transition-all duration-300 flex flex-col justify-between relative overflow-hidden">
          <div class="absolute top-0 right-0 w-32 h-32 bg-amber-100/50 rounded-bl-full pointer-events-none group-hover:scale-110 transition-transform"></div>

          <div class="relative z-10 space-y-4">
            <div class="w-14 h-14 rounded-2xl bg-amber-100 text-amber-700 flex items-center justify-center text-2xl shadow-xs group-hover:rotate-6 transition-transform">
              🍜
            </div>
            <div>
              <span class="text-[11px] font-extrabold text-amber-800 uppercase tracking-wider bg-amber-50 px-2.5 py-1 rounded-md border border-amber-200/60 inline-block mb-2">Everyday Life</span>
              <h2 class="text-xl sm:text-2xl font-extrabold text-slate-900 group-hover:text-amber-700 transition-colors font-brand">Daily Conversations</h2>
              <p class="text-sm sm:text-base text-stone-600 leading-relaxed mt-2 font-normal">
                Use natural phrase templates for ordering food, shopping, and doing everyday small talk.
              </p>
            </div>
          </div>

          <div class="mt-8 pt-4 border-t border-stone-200/70 flex items-center justify-between text-xs sm:text-sm font-bold text-slate-800 group-hover:text-amber-700 transition-colors relative z-10">
            <span>Launch Formula</span>
            <span class="group-hover:translate-x-1.5 transition-transform text-base">→</span>
          </div>
        </a>

      </div>
    </section>

  </main>

  <!-- FOOTER -->
  <footer class="bg-[#fffaf5] text-stone-600 py-10 border-t border-stone-200/80">
    <div class="max-w-7xl mx-auto px-5 sm:px-8 lg:px-12 flex flex-col md:flex-row items-center justify-between gap-6 text-center md:text-left">
      <div class="flex items-center justify-center gap-2.5">
        <span class="font-brand font-black text-slate-900 tracking-wider text-base sm:text-lg">HAVIE</span>
        <span class="text-stone-300">•</span>
        <span class="text-xs sm:text-sm font-medium text-stone-500">Learn Vietnamese with Hang</span>
      </div>
      <div class="flex items-center gap-5 sm:gap-7 font-semibold text-xs sm:text-sm text-stone-700 flex-wrap justify-center">
        <a href="/#about" class="hover:text-red-600 transition">About Me</a>
        <a href="/#curriculum" class="hover:text-red-600 transition">Learning Tracks</a>
        <a href="/resources" class="hover:text-red-600 transition font-bold">Resources</a>
        <a href="/student" class="hover:text-red-600 transition">Student Portal</a>
        <a href="/test" class="hover:text-red-600 transition">Level Test</a>
        <button type="button" class="open-modal-trigger text-red-600 font-bold hover:underline cursor-pointer">Book Trial</button>
      </div>
    </div>
  </footer>

  <!-- CONSULTATION / BOOKING MODAL -->
  <div id="consultModal" class="hidden fixed inset-0 z-50 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4">
    <div class="bg-white rounded-3xl p-6 sm:p-8 max-w-lg w-full shadow-2xl border border-stone-200 space-y-6 text-left">
      
      <div class="flex items-center justify-between border-b border-stone-100 pb-4">
        <div>
          <span class="text-xs sm:text-sm font-black text-red-600 uppercase tracking-widest bg-red-50 px-3.5 py-1.5 rounded-lg border border-red-200 inline-block font-brand">1-ON-1 LESSONS</span>
          <h3 class="text-2xl sm:text-3xl font-black text-slate-900 font-brand mt-1.5">Book 25-Min Trial Lesson</h3>
        </div>
        <button id="closeModalBtn" type="button" class="w-10 h-10 rounded-full bg-stone-100 hover:bg-stone-200 text-stone-700 font-bold flex items-center justify-center text-lg cursor-pointer">
          ✕
        </button>
      </div>

      <form id="consultForm" class="space-y-4 text-sm">
        <div>
          <label class="block font-bold text-slate-800 mb-1.5 text-base">Full Name / Tên của bạn *</label>
          <input type="text" id="cName" required placeholder="e.g. Alex Smith" class="w-full px-4 py-3.5 rounded-xl border border-stone-300 focus:outline-none focus:border-red-500 bg-stone-50/50 text-base" />
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label class="block font-bold text-slate-800 mb-1.5 text-base">Country / Quốc gia *</label>
            <input type="text" id="cCountry" required placeholder="e.g. Germany, USA" class="w-full px-4 py-3.5 rounded-xl border border-stone-300 focus:outline-none focus:border-red-500 bg-stone-50/50 text-base" />
          </div>
          <div>
            <label class="block font-bold text-slate-800 mb-1.5 text-base">Phone / SĐT</label>
            <input type="tel" id="cPhone" placeholder="e.g. +49..." class="w-full px-4 py-3.5 rounded-xl border border-stone-300 focus:outline-none focus:border-red-500 bg-stone-50/50 text-base" />
          </div>
        </div>

        <div>
          <label class="block font-bold text-slate-800 mb-1.5 text-base">Email Address *</label>
          <input type="email" id="cEmail" required placeholder="name@example.com" class="w-full px-4 py-3.5 rounded-xl border border-stone-300 focus:outline-none focus:border-red-500 bg-stone-50/50 text-base" />
        </div>

        <div>
          <label class="block font-bold text-slate-800 mb-1.5 text-base">Learning Goals / Mục tiêu học tập *</label>
          <textarea id="cGoals" rows="3" required placeholder="Current level, target dialect (Hanoi/Saigon), goals..." class="w-full px-4 py-3.5 rounded-xl border border-stone-300 focus:outline-none focus:border-red-500 bg-stone-50/50 resize-none text-base"></textarea>
        </div>

        <button
          type="submit"
          class="w-full py-4 bg-red-600 hover:bg-red-700 text-white font-black rounded-xl text-base shadow-md transition cursor-pointer mt-3"
        >
          Submit Request to Hang →
        </button>
      </form>

      <div id="successMessage" class="hidden text-center py-6 space-y-3">
        <div class="text-4xl">🎉</div>
        <h4 class="font-black text-slate-900 text-lg font-brand">Request Sent Successfully!</h4>
        <p class="text-stone-600 text-sm leading-relaxed">Thank you! Hang has received your details and goals. She will reach out via email within 24 hours.</p>
        <button id="btnCloseSuccess" type="button" class="px-6 py-3 bg-slate-900 text-white font-bold rounded-xl text-sm cursor-pointer">
          Close Window
        </button>
      </div>

    </div>
  </div>

  <!-- SCRIPTS -->
  <script is:inline>
    // Mobile Menu Toggle
    const mobileMenuBtn = document.getElementById('mobileMenuBtn');
    const mobileMenu = document.getElementById('mobileMenu');
    const mobileLinks = document.querySelectorAll('.mobile-link');
    if (mobileMenuBtn && mobileMenu) {
      mobileMenuBtn.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
      });
      mobileLinks.forEach(link => {
        link.addEventListener('click', () => mobileMenu.classList.add('hidden'));
      });
    }

    // Modal Control Script
    const modal = document.getElementById('consultModal');
    const openTriggers = document.querySelectorAll('.open-modal-trigger');
    const closeModalBtn = document.getElementById('closeModalBtn');
    const consultForm = document.getElementById('consultForm');
    const successMessage = document.getElementById('successMessage');
    const btnCloseSuccess = document.getElementById('btnCloseSuccess');

    openTriggers.forEach(btn => {
      if (btn) {
        btn.addEventListener('click', () => {
          modal.classList.remove('hidden');
          consultForm.classList.remove('hidden');
          successMessage.classList.add('hidden');
          if (mobileMenu) mobileMenu.classList.add('hidden');
        });
      }
    });

    const hideModal = () => {
      modal.classList.add('hidden');
      if (consultForm && successMessage) {
        consultForm.classList.remove('hidden');
        successMessage.classList.add('hidden');
        consultForm.reset();
      }
    };

    if (closeModalBtn) closeModalBtn.addEventListener('click', hideModal);
    if (btnCloseSuccess) closeModalBtn.addEventListener('click', hideModal);
    modal.addEventListener('click', (e) => { if (e.target === modal) hideModal(); });

    // FORM SUBMISSION
    consultForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const submitBtn = consultForm.querySelector('button[type="submit"]');
      if (submitBtn.disabled) return;

      const name = document.getElementById('cName').value;
      const country = document.getElementById('cCountry').value;
      const phone = document.getElementById('cPhone').value;
      const email = document.getElementById('cEmail').value;
      const goals = document.getElementById('cGoals').value;

      const WEB_APP_URL = 'https://script.google.com/macros/s/AKfycbwALyCIFh4XLjpYQCaJxQH3S3krOkcRJERXz9IcAPWpUK3QKOpj583xWw6A31TfgvSDqw/exec'; 

      const formData = new URLSearchParams();
      formData.append('name', name);
      formData.append('country', country);
      formData.append('phone', phone);
      formData.append('email', email);
      formData.append('goals', goals);

      submitBtn.disabled = true;
      submitBtn.style.opacity = '0.7';
      submitBtn.style.cursor = 'not-allowed';
      const originalText = submitBtn.innerHTML;
      submitBtn.innerHTML = 'Sending request... ⏳';

      try {
        await fetch(WEB_APP_URL, {
          method: 'POST',
          mode: 'no-cors',
          body: formData
        });

        consultForm.classList.add('hidden');
        successMessage.classList.remove('hidden');

      } catch (error) {
        console.error("Error submitting form:", error);
        consultForm.classList.add('hidden');
        successMessage.classList.add('hidden');
      } finally {
        submitBtn.disabled = false;
        submitBtn.style.opacity = '1';
        submitBtn.style.cursor = 'pointer';
        submitBtn.innerHTML = originalText;
      }
    });
  </script>
</BaseLayout>